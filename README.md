# Demo Express Project

This is a minimal Express demo with a single health-check endpoint.

## Setup

Install dependencies:

```bash
npm install
```

## Run

Start the server:

```bash
npm start
```

By default it listens on port 3000.

## Endpoint

- `GET /status` → returns plain text `ok`

